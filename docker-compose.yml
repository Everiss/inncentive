version: '3.8'

services:
  valkey:
    image: valkey/valkey:latest
    container_name: new-tax-valkey
    ports:
      - "6379:6379"
    volumes:
      - valkey_data:/data
    restart: unless-stopped

volumes:
  valkey_data:
